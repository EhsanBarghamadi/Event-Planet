$kind: http-request
url: "{{baseUrl}}/api/v1/user/logout/"
method: POST
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/v1 user logout create.resources/examples
order: 1000
