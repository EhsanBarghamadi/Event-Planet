$kind: http-request
url: "{{baseUrl}}/api/v1/user/token/logout/"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |
    {
      "refresh": "string"
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/v1 user token logout create.resources/examples
order: 1000
