$kind: http-request
url: "{{baseUrl}}/api/v1/result/?search=string"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: search
    value: string
    description: A search term.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/v1 result list.resources/examples
order: 2000
