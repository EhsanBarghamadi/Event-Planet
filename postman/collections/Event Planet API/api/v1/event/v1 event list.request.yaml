$kind: http-request
url: "{{baseUrl}}/api/v1/event/"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/v1 event list.resources/examples
order: 2000
